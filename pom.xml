<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>eu.opensoftware.hawtio</groupId>
  <artifactId>activemq-classic-hawtio</artifactId>
  <version>2.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <!-- Plugin metadata -->
    <plugin.id>activemq-classic</plugin.id>
    <plugin.scope>activemqClassic</plugin.scope>
    <plugin.module>plugin</plugin.module>

    <!-- Java -->
    <maven.compiler.source>17</maven.compiler.source>
    <maven.compiler.target>17</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Dependency versions -->
    <hawtio.plugin.api.version>5.2.0</hawtio.plugin.api.version>
    <jakarta.servlet.version>5.0.0</jakarta.servlet.version>
    <jakarta.servlet-api61.version>6.1.0</jakarta.servlet-api61.version>

    <slf4j.version>2.0.11</slf4j.version>

    <!-- Frontend -->
    <node.version>v20.19.0</node.version>
    <yarn.version>4.15.0</yarn.version> <!-- Yarn Berry -->

    <!-- Maven plugins -->
    <maven.resources.plugin.version>3.5.0</maven.resources.plugin.version>
    <maven.war.plugin.version>3.5.1</maven.war.plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.hawt</groupId>
        <artifactId>hawtio-bom</artifactId>
        <version>${hawtio.plugin.api.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

    <!-- Servlet API (necessaria per @WebListener, ServletContextListener, ecc.) -->
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <version>${jakarta.servlet.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Hawtio plugin API (necessaria per HawtioPlugin) -->
    <dependency>
      <groupId>io.hawt</groupId>
      <artifactId>hawtio-plugin-mbean</artifactId>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
      <scope>provided</scope>
    </dependency>

  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>plugin.properties</include>
        </includes>
        <filtering>true</filtering>
      </resource> 
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>META-INF/web-fragment.xml</include>
        </includes>
      </resource>
    </resources>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>3.6.3</version>
          <executions>
            <execution>
              <id>enable-corepack</id>
              <phase>generate-resources</phase>
              <goals>
                <goal>exec</goal>
              </goals>
            </execution>
            <execution>
              <id>yarn-install</id>
              <phase>generate-resources</phase>
              <goals>
                <goal>exec</goal>
              </goals>
            </execution>
            <execution>
              <id>yarn-build</id>
              <phase>generate-resources</phase>
              <goals>
                <goal>exec</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.6.3</version>

        <executions>

          <!-- Enable corepack-->
          <execution>
            <id>enable-corepack</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <workingDirectory>${project.basedir}/frontend</workingDirectory>
              <executable>corepack</executable>
              <arguments>
                <argument>enable</argument>
              </arguments>
            </configuration>
          </execution>

          <!-- Install FE deps -->
          <execution>
            <id>yarn-install</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <workingDirectory>${project.basedir}/frontend</workingDirectory>
              <executable>yarn</executable>
              <arguments>
                <argument>install</argument>
                <argument>--immutable</argument>
              </arguments>
            </configuration>
          </execution>

          <!-- Build FE -->
          <execution>
            <id>yarn-build</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <workingDirectory>${project.basedir}/frontend</workingDirectory>
              <environmentVariables>
                <PLUGIN_SCOPE>${plugin.scope}</PLUGIN_SCOPE>
                <PLUGIN_MODULE>${plugin.module}</PLUGIN_MODULE>
              </environmentVariables>
              <executable>yarn</executable>
              <arguments>
                <argument>build</argument>
              </arguments>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <!-- Copia plugin FE nel WAR -->
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${maven.resources.plugin.version}</version>
        <executions>

          <!-- Copia plugin FE nel WAR -->
          <execution>
            <id>copy-frontend</id>
            <phase>process-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <encoding>${project.build.sourceEncoding}</encoding>
              <outputDirectory>${project.build.outputDirectory}/${plugin.id}</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/frontend/build/</directory>
                  <includes>
                    <include>*.js</include>
                    <include>*.css</include>
                    <include>*.map</include>
                    <include>static/**</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>

          <!-- Filtra package.json.template -->
          <execution>
            <id>filter-package-json</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}/frontend</outputDirectory>
              <resources>
                <resource>
                  <directory>${project.basedir}/frontend</directory>
                  <includes>
                    <include>package.json.template</include>
                  </includes>
                  <filtering>true</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <!-- WAR plugin -->
      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>${maven.war.plugin.version}</version>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>

    </plugins>
  </build>

</project>
